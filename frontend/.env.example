# Frontend env. Copy to frontend/.env and fill in real values.
# Vite only exposes vars prefixed with VITE_ to the browser.
# The backend's RPC URL and DB creds are NEVER read here.

# Empty = use the Vite dev proxy (recommended). Set to a full origin
# only if you're hosting the frontend separately from the backend.
VITE_API_BASE=
